name: Build and Release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  build:
    name: Build Multi-Platform Binaries
    runs-on: ${{ matrix.os }}
    defaults:
      run:
        shell: bash
    strategy:
      matrix:
        os: [windows-latest, ubuntu-latest, macos-latest]

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Compile Game
        run: |
          mkdir -p bin
          find src -name "*.java" > sources.txt
          javac -encoding UTF-8 -d bin @sources.txt

      - name: Create JAR
        run: |
          cp resources/fonts/*.ttf bin/ || true
          cp resources/app_logo.png bin/ || true
          cd bin
          echo "Main-Class: Main" > manifest.txt
          jar cfm ../Samarbhumi.jar manifest.txt .
          cd ..

      - name: Packaging (Windows - Full Setup Installer)
        if: matrix.os == 'windows-latest'
        run: |
          jpackage --type exe --name "Samarbhumi-Setup" --input . --main-jar Samarbhumi.jar --main-class Main --win-shortcut --win-menu --win-dir-chooser --license-file INSTALLER_INFO.txt --app-version 1.2 --vendor "Samarbhumi" --description "A fast-paced 2D action shooter — War Never Ends" --verbose

      - name: Packaging (macOS - App Bundle & Disk Image)
        if: matrix.os == 'macos-latest'
        run: |
          jpackage --type dmg --name "Samarbhumi" --input . --main-jar Samarbhumi.jar --main-class Main --license-file INSTALLER_INFO.txt --app-version 1.2 --vendor "Samarbhumi" --mac-package-name "Samarbhumi" --verbose

      - name: Packaging (Linux - Debian Package)
        if: matrix.os == 'ubuntu-latest'
        run: |
          jpackage --type deb --name "samarbhumi" --input . --main-jar Samarbhumi.jar --main-class Main --license-file INSTALLER_INFO.txt --app-version 1.2 --vendor "Samarbhumi" --linux-shortcut --description "A fast-paced 2D action shooter — War Never Ends" --verbose

      - name: Create Release and Upload
        uses: softprops/action-gh-release@v2
        if: startsWith(github.ref, 'refs/tags/')
        with:
          files: |
            *.exe
            *.msi
            *.dmg
            *.deb
            Samarbhumi.jar
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
